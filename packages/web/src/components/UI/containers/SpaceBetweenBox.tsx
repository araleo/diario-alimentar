import { styled } from '@mui/material';

import FlexRowBox from './FlexRowBox';

const SpaceBetweenBox = styled(FlexRowBox)({
  alignItems: 'center',
  justifyContent: 'space-between',
});

export default SpaceBetweenBox;
