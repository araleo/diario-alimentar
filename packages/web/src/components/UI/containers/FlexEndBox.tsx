import { styled } from '@mui/material';

import FlexRowBox from './FlexRowBox';

const FlexEndBox = styled(FlexRowBox)({
  alignItems: 'center',
  justifyContent: 'flex-end',
});

export default FlexEndBox;
