import { styled } from '@mui/material';

import FlexRowBox from './FlexRowBox';

const SpaceAroundBox = styled(FlexRowBox)({
  alignItems: 'center',
  justifyContent: 'space-around',
});

export default SpaceAroundBox;
